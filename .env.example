# PETROSIMX - Environment Variables Configuration
# Note: The core process simulation engine is 100% browser-native & serverless (zero cloud lock-in).
# The variables below are reserved for future optional integration if cloud storage or AI advisory features are requested.

# Optional: For future AI process optimization or smart narrative advisory
GEMINI_API_KEY=""

# Optional: Host URL if deployed with reverse proxy
APP_URL=""

